use std::io::{stdin, Read};

pub fn p01() {
    let mut input = String::new();
    stdin().read_to_string(&mut input).unwrap();
}

pub fn p02() {
    let mut input = String::new();
    stdin().read_to_string(&mut input).unwrap();
}
